# -- Server -------------------------------------------------------------------
PORT=3000

# -- Database -----------------------------------------------------------------
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

# -- Token --------------------------------------------------------------------
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# -- Security -----------------------------------------------------------------
SALT_ROUNDS=12
SENTRI_API_KEY=your-sentri-api-key

# -- Redis (optional) ---------------------------------------------------------
# REDIS_URL=redis://localhost:6379
